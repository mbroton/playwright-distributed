components:
  schemas:
    BrowserCapacity:
      additionalProperties: false
      properties:
        active_sessions:
          description: Fleet-wide active sessions, including sessions on ineligible workers.
          format: int64
          type: integer
        available_slots:
          description: Free slots on eligible workers; can exceed max_slots - active_sessions, which is fleet-wide.
          format: int64
          type: integer
        browser:
          type: string
        max_slots:
          format: int64
          type: integer
        workers:
          format: int64
          type: integer
      required:
        - browser
        - workers
        - max_slots
        - active_sessions
        - available_slots
      type: object
    Capacity:
      additionalProperties: false
      properties:
        browsers:
          items:
            $ref: "#/components/schemas/BrowserCapacity"
          type: array
        max_queue_size:
          format: int64
          type: integer
        queued:
          description: Queue depth on this server replica.
          format: int64
          type: integer
        totals:
          $ref: "#/components/schemas/CapacityTotals"
      required:
        - browsers
        - totals
        - queued
        - max_queue_size
      type: object
    CapacityTotals:
      additionalProperties: false
      properties:
        active_sessions:
          description: Fleet-wide active sessions, including sessions on ineligible workers.
          format: int64
          type: integer
        available_slots:
          description: Free slots on eligible workers; can exceed max_slots - active_sessions, which is fleet-wide.
          format: int64
          type: integer
        max_slots:
          format: int64
          type: integer
        workers:
          format: int64
          type: integer
      required:
        - workers
        - max_slots
        - active_sessions
        - available_slots
      type: object
    CreateSessionInputBody:
      additionalProperties: false
      properties:
        browser:
          enum:
            - chromium
            - firefox
            - webkit
          type: string
        connect_metadata:
          additionalProperties: {}
          type: object
        mode:
          default: default
          enum:
            - default
            - dedicated
          type: string
      required:
        - browser
      type: object
    ErrorDetail:
      additionalProperties: false
      properties:
        location:
          description: Where the error occurred, e.g. 'body.items[3].tags' or 'path.thing-id'
          type: string
        message:
          description: Error message text
          type: string
        value:
          description: The value at the given location
      type: object
    ErrorModel:
      additionalProperties: false
      properties:
        detail:
          description: A human-readable explanation specific to this occurrence of the problem.
          examples:
            - Property foo is required but is missing.
          type: string
        errors:
          description: Optional list of individual error details
          items:
            $ref: "#/components/schemas/ErrorDetail"
          type:
            - array
            - "null"
        instance:
          description: A URI reference that identifies the specific occurrence of the problem.
          examples:
            - https://example.com/error-log/abc123
          format: uri
          type: string
        status:
          description: HTTP status code
          examples:
            - 400
          format: int64
          type: integer
        title:
          description: A short, human-readable summary of the problem type. This value should not change between occurrences of the error.
          examples:
            - Bad Request
          type: string
        type:
          default: about:blank
          description: A URI reference to human-readable documentation for the error.
          examples:
            - https://example.com/errors/example
          format: uri
          type: string
      type: object
    HealthOutputBody:
      additionalProperties: false
      properties:
        status:
          type: string
      required:
        - status
      type: object
    HeartbeatInputBody:
      additionalProperties: false
      properties:
        active_session_ids:
          items:
            format: uuid
            type: string
          type: array
      required:
        - active_session_ids
      type: object
    HeartbeatOutputBody:
      additionalProperties: false
      properties:
        commands:
          items:
            type: string
          type: array
        stale_session_ids:
          items:
            format: uuid
            type: string
          type: array
        status:
          enum:
            - available
            - draining
            - stalled
            - shutting_down
          type: string
      required:
        - status
        - commands
        - stale_session_ids
      type: object
    RegisterWorkerInputBody:
      additionalProperties: false
      properties:
        address:
          format: uri
          maxLength: 512
          pattern: ^wss?://[^\s/?#]+
          type: string
        browser:
          enum:
            - chromium
            - firefox
            - webkit
          type: string
        max_slots:
          format: int32
          maximum: 1024
          minimum: 1
          type: integer
        playwright_version:
          maxLength: 64
          minLength: 1
          type: string
      required:
        - address
        - browser
        - playwright_version
        - max_slots
      type: object
    Session:
      additionalProperties: false
      properties:
        browser:
          type: string
        connect_metadata:
          additionalProperties: {}
          type: object
        created_at:
          format: date-time
          type: string
        created_by_key:
          format: uuid
          type: string
        expires_at:
          format: date-time
          type: string
        id:
          format: uuid
          type: string
        keep_alive_ms:
          format: int32
          type: integer
        last_heartbeat:
          format: date-time
          type: string
        mode:
          enum:
            - default
            - dedicated
          type: string
        playwright_version:
          type: string
        started_at:
          format: date-time
          type:
            - string
            - "null"
        status:
          enum:
            - pending
            - running
            - completed
            - failed
            - expired
          type: string
        worker_address:
          type: string
        worker_id:
          format: uuid
          type: string
      required:
        - id
        - worker_id
        - browser
        - playwright_version
        - worker_address
        - mode
        - status
        - created_at
        - started_at
        - last_heartbeat
        - connect_metadata
      type: object
    SetStatusInputBody:
      additionalProperties: false
      properties:
        status:
          enum:
            - draining
            - shutting_down
          type: string
      required:
        - status
      type: object
    Worker:
      additionalProperties: false
      properties:
        address:
          type: string
        browser:
          type: string
        created_at:
          format: date-time
          type: string
        id:
          format: uuid
          type: string
        last_heartbeat:
          format: date-time
          type: string
        lifetime_sessions:
          format: int64
          type: integer
        max_slots:
          format: int32
          type: integer
        playwright_version:
          type: string
        status:
          enum:
            - available
            - draining
            - stalled
            - shutting_down
          type: string
      required:
        - id
        - address
        - browser
        - playwright_version
        - max_slots
        - status
        - last_heartbeat
        - lifetime_sessions
        - created_at
      type: object
  securitySchemes:
    bearer:
      bearerFormat: pwd_<random>
      scheme: bearer
      type: http
info:
  title: Playwright Distributed Control Plane
  version: 1.0.0
openapi: 3.1.0
paths:
  /healthz:
    get:
      operationId: health
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Check process health
      tags:
        - Infrastructure
  /internal/workers:
    post:
      operationId: register-worker
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterWorkerInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Worker"
          description: Created
        "401":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Unauthorized
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Internal Server Error
        "503":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Service Unavailable
      security:
        - bearer: []
      summary: Register a worker
      tags:
        - Workers
  /internal/workers/{id}/heartbeat:
    post:
      operationId: heartbeat-worker
      parameters:
        - description: Worker ID
          in: path
          name: id
          required: true
          schema:
            description: Worker ID
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HeartbeatInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HeartbeatOutputBody"
          description: OK
        "401":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Unauthorized
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Internal Server Error
        "503":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Service Unavailable
      security:
        - bearer: []
      summary: Heartbeat a worker
      tags:
        - Workers
  /internal/workers/{id}/status:
    post:
      operationId: set-worker-status
      parameters:
        - description: Worker ID
          in: path
          name: id
          required: true
          schema:
            description: Worker ID
            format: uuid
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetStatusInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Worker"
          description: OK
        "401":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Unauthorized
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Internal Server Error
        "503":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Service Unavailable
      security:
        - bearer: []
      summary: Request a worker status transition
      tags:
        - Workers
  /readyz:
    get:
      operationId: readiness
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthOutputBody"
          description: OK
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Internal Server Error
        "503":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Service Unavailable
      summary: Check database readiness
      tags:
        - Infrastructure
  /v1/capacity:
    get:
      operationId: get-capacity
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Capacity"
          description: OK
        "401":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Unauthorized
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Internal Server Error
        "503":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Service Unavailable
      security:
        - bearer: []
      summary: Get session capacity
      tags:
        - Sessions
  /v1/sessions:
    post:
      operationId: create-session
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSessionInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
          description: Created
        "401":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Unauthorized
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Unprocessable Entity
        "429":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Too Many Requests
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Internal Server Error
        "503":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Service Unavailable
      security:
        - bearer: []
      summary: Create a session
      tags:
        - Sessions
  /v1/sessions/{id}:
    get:
      operationId: get-session
      parameters:
        - description: Session ID
          in: path
          name: id
          required: true
          schema:
            description: Session ID
            format: uuid
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
          description: OK
        "401":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Unauthorized
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Internal Server Error
        "503":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Service Unavailable
      security:
        - bearer: []
      summary: Get a session
      tags:
        - Sessions
  /v1/workers:
    get:
      operationId: list-workers
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/Worker"
                type: array
          description: OK
        "401":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Unauthorized
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Internal Server Error
        "503":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Service Unavailable
      security:
        - bearer: []
      summary: List workers
      tags:
        - Workers
