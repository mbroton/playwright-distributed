# Copy to .env before a real deployment of docker-compose.yaml.

# PostgreSQL password. Read only when the postgres-data volume is first
# created; changing it later requires resetting the volume.
POSTGRES_PASSWORD=

# API key for the workers. Required as soon as the server has any API key:
# docker compose exec server server apikey create --name <name>
WORKER_API_KEY=
